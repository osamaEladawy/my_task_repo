class BottomAppBarModel {
  final int? id;
  final String? title;
  // final IconData? icon;
  // final IconData? activeIcon;

  BottomAppBarModel({
    required this.id,
    required this.title,
    // required this.icon,
    // required this.activeIcon,
  });
}
